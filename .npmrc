# Ignore engine warnings for transitive dependencies
engine-strict=false

# Set registry
registry=https://registry.npmjs.org/

# Use new npm options instead of deprecated ones
omit=dev
include=optional 