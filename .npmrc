# Ignore engine warnings for transitive dependencies
engine-strict=false

# Set registry
registry=https://registry.npmjs.org/

# Don't omit dev dependencies during build as some are needed for static generation
# omit=dev
# include=optional 